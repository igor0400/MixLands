import { FC } from 'react';

const Wiki: FC = () => {
   return (
      <div className="wiki">
         <h2 className="text-center mt-12 text-2xl font-semibold text-gray-500">
            Временно недоступно
         </h2>
      </div>
   );
};

export default Wiki;
